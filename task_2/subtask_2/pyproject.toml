[project]
name = "subtask-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "zahabyyy", email = "ahmedelzahaby86@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
]

[project.scripts]
subtask-2 = "subtask_2:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
